# Frontend - junior hunt

### Инструкция по развертыванию проекта:

1. Установка зависимостей `yarn install` или же `npm install`

Для запуска приложения использовать команду `yarn start` или же `npm start`